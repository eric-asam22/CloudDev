# Challenge: Anagram Check

# Write the function is_anagram that takes two strings as input and returns True if the strings are anagrams of each other,
# and False otherwise. Anagrams are strings that contain the same characters but may be in a different order.

def is_anagram(str1, str2):
   if(len(str1)!=(str2)): return False
   else
        sorted_str1= ''.join()


# Test the function
input_str1 = "listen"
input_str2 = "silent"
expected_output = True
output = is_anagram(input_str1, input_str2)
print(output == expected_output)
